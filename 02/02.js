console.log("Hello, World!")

function veletlenSzamGenerator() {
    return new Promise ((resolve)=> {
        const veletlenSzam = Math.floor(Math.random()*100)+ 1;
        const veletlenIdo = Math.floor(Math.random()*5000) + 1000

        setTimeout(() => {
            console.log
        }, timeout);
    })
}